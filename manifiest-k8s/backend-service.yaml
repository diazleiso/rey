apiVersion: v1
kind: Service
metadata:
  name: backend-piloto-service
  namespace: reybanpac-piloto
  labels:
    app: backend-piloto
    environment: development
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: backend-piloto
